CREATE TABLE `email_aliasses` (
    `id` BIGINT UNSIGNED NOT NULL,
    `dialect` BIGINT UNSIGNED NOT NULL COMMENT 'foreign to world_dialects.id',
     UNIQUE KEY `id` (`id`),
     KEY `index` (`id`)
)  ENGINE=InnoDB DEFAULT CHARSET=utf8 COLLATE=utf8_unicode_ci;
